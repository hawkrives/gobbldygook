---
general:
  branches:
    ignore:
      - gh-pages

machine:
  node:
    version: 7
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  override:
    - npm i
  cache_directories:
    - ~/.npm

test:
  override:
    - npm test -- --runInBand
